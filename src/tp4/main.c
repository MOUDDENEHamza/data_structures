#include <stdio.h>
#include "libMath.h"

/*Main function*/
int main(int argc, char *argv[]) {
	float x;
	float y;
	float z;
	
	/*Start program*/
	input_point(float &x, float &y, float &z);//Ask user to input the coordinates.
       	create_point(float &x, float &y, float &z);//Create point by the coordinates entred.	

	return 0;
}
